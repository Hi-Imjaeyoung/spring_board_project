spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/board
    username: root
    password: 1234
  jpa:
    database: mysql
    #    innoDB ???? ??     #jap? ?? ???
    database-platform: org.hibernate.dialect.MariaDBDialect
    #    ???? ?? ?? ??!
    generate-ddl: true
    hibernate:
      #      create ??? ?? ???? drop? ?? > ?? ?? ?? ?? (?? ??..) ?? ??? ? ??.
      #      update ??? ????? alter? ?? > ?? ?? ??? ?? ??? > raw DDL ????? ??
      #      validate? ??DB?? ???? check > DB ?? ?? x
      #      ????? update ?? validate ??
      ddl-auto: update
    #   jpa? ???? ??? ????, ??? console?? ??????
    show_sql: true
